doctype html
html(lang="ko")
    head
        meta(charset="UTF-8")
        meta(name="viewport", content="width=device-width, initial-scale=1.0")
        title CoolChat
        link(rel="stylesheet", href="https://unpkg.com/mvp.css")
    body 
        header 
            h1 CoolChat
        main 
            div#welcome
                form#roomForm
                    input#name(type="text", placeholder="닉네임 입력", required)
                    input#roomNum(type="text", placeholder="방 이름", required)
                    button 참가
                h4 현재 생성된 방 [<span></span>] 👇      
                ul
            div#room 
                h3
                ul
                form#msg
                    input(type="text", placeholder="메세지 입력", required)
                    button 전송
        //- socket.io 추가
        script(src="/socket.io/socket.io.js") 
        script(src="/public/js/app.js") 